/** @type {import('next').NextConfig} */
const nextConfig = {
  // Throwaway harness — keep it simple; no image optimization, no exotic config.
  reactStrictMode: true,
};

export default nextConfig;
